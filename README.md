# Node.js CI/CD Demo

Complete GitHub Actions pipeline for Node.js + Docker

## Quick Start

1. npm install
2. npm test
3. npm start

Visit http://localhost:3000

## CI/CD Pipeline

- Push to main → tests run → Docker image built → deployed to staging
- Pull requests run tests only
